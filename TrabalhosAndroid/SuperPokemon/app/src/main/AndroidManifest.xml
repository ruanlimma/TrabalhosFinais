<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin2.superpokemon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Pokemon Battle"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".Escolha1Activity" />
        <activity android:name=".JogadaActivity" />
        <activity android:name=".PerdeuActivity" />
        <activity android:name=".GanhouActivity" />
        <activity android:name=".VelhaActivity" />

        <service
            android:name="Game_jvelha"
            android:enabled="false"
            android:exported="false"></service>
    </application>

</manifest>